import {ArrowLeft,Phone,Video,Search,MoreVertical,PanelRightOpen} from 'lucide-react';import Avatar from '../common/Avatar';import {useChat} from '../../context/ChatContext';export default function ChatHeader(){const {selectedUser,setSelected,setDetailsOpen,notify}=useChat();return <header className="chat-head"><button className="mobile-only round" onClick={()=>setSelected(null)} aria-label="Back"><ArrowLeft/></button><Avatar user={selectedUser} online={selectedUser?.status==='Online'}/><div className="chat-person"><b>{selectedUser?.name}</b><span className={selectedUser?.status==='Online'?'online-text':''}>● {selectedUser?.status||'Offline'} {selectedUser?.isGroup?'• 8 members':''}</span></div><div className="head-actions"><button onClick={()=>notify('Voice call demo')}><Phone/></button><button onClick={()=>notify('Video call demo')}><Video/></button><button onClick={()=>notify('Message search demo')}><Search/></button><button onClick={()=>setDetailsOpen(v=>!v)}><PanelRightOpen/></button><button onClick={()=>notify('More chat options')}><MoreVertical/></button></div></header>}
