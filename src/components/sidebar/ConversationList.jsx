import ConversationItem from './ConversationItem';import {useChat} from '../../context/ChatContext';export default function ConversationList(){const {visible,userById}=useChat();return <div className="conversation-list">{visible.length?visible.map(c=><ConversationItem key={c.id} conv={c} user={userById(c.userId)}/>):<div className="empty mini">🔍<b>No conversations found</b><span>Try another search or filter.</span></div>}</div>}
